from imports import *

def logout():
  res = requests.post('http://localhost:9103/logout',
                      headers=find_key(),
                      verify=False)
  print(res.status_code)
  if (res.status_code == 200):
    if os.path.exists(home + '/softeng22API.token'):
      os.remove(home + '/softeng22API.token')
  return True

logout()